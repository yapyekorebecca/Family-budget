# ── Frontend API URL ─────────────────────────────────────────────────────────
# Point this at your Railway backend URL in production.
# In local dev, leave it empty (defaults to http://127.0.0.1:8000)
VITE_API_BASE_URL=https://your-railway-domain.up.railway.app
